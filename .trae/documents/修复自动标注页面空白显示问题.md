## 问题定位
- 页面当前模版可能仍为自闭合：`src/views/demo/automatic-annotation/index.vue:1-4` 显示 `<common-page ... />`，没有插槽内容，因此卡片主体为空
- `CommonPage` 组件会在插槽位置渲染内容：`src/components/common/CommonPage.vue:32-34`；如果没有内容，页面看起来就是空白
- 路由已指向该页面：`src/store/helper.js:233-243`，访问路径为 `/demo/automatic-annotation`

## 计划修复
- 将页面模板改为在 `common-page` 插槽中渲染组件：
  - 在 `src/views/demo/automatic-annotation/index.vue` 使用：
    ```vue
    <template>
      <common-page :show-header="false" :show-footer="false">
        <ImageAnnotator />
      </common-page>
    </template>
    <script setup>
    import ImageAnnotator from './components/ImageAnnotator.vue'
    </script>
    ```
- 保持 `ImageAnnotator` 的已有实现不变；它依赖的 Naive UI 标签由 `vite.config.js:29-32` 自动解析，无需手动注册
- 验证：进入 `/demo/automatic-annotation`，应看到工具栏与画布；如仍为空白，进行以下自检：
  - 浏览器控制台是否有运行时错误（例如组件未找到）
  - 强制刷新避免缓存（Ctrl/Cmd+Shift+R）
  - 视图高度问题：如容器高度异常，可将 `ImageAnnotator.vue` 的 `.annotator` 高度从 `calc(100vh - 20px)` 改为 `height: 100%` 以贴合 `CommonPage` 的卡片高度

## 交付与验证
- 更新页面模板并导入组件
- 本地访问 `/demo/automatic-annotation` 完成交互验证：上传、矩形标注、拖拽缩放、导出 JSON/PNG，无报错
