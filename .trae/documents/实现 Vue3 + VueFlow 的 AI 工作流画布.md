## 目标
- 升级节点与边的视觉与交互，做成卡片式节点、箭头边、状态与标签显示。
- 增加左侧节点库、右侧属性面板、顶部工具栏；完善增删改连、缩放与视图辅助。
- 支持导入/导出 JSON、连接校验与简单运行模拟，贴近扣子 AI 体验。

## 主要改造
- SpecialNode：卡片头部图标与标题、状态徽标、参数/标签预览，左右 Handle；悬浮/选中态高亮。
- SpecialEdge：平滑曲线+箭头、状态颜色与宽度、标签渲染；运行时动画；悬浮/选中高亮。
- index.vue：
  - 顶部工具栏：返回、缩放、fitView、导入/导出、运行模拟、重置。
  - 左侧节点库：输入/LLM/工具/分支/输出，点击添加到画布。
  - 右侧属性面板：选中节点时 `n-form` 编辑标题、描述、参数（LLM/工具/分支）。
  - 事件：选择/连接/删除/复制、连接校验（禁止自连）、运行模拟状态流转。

## 数据结构
- 节点 `data`：`{ title, icon, type, typeStyle, status, params, variables, description }`。
- 边 `data`：`{ label, status }`。

## 交互
- 初始 `fitView`、缩放范围限制、网格背景。
- 快捷：Delete 删除、Cmd+D 复制，Enter 重命名（基础版）。

确认后我将直接开始实现与验证。